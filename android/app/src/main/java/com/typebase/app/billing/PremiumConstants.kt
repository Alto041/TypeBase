package com.typebase.app.billing

/** Play Console one-time in-app product ID — create as non-consumable before release. */
object PremiumConstants {
  const val PREMIUM_SKU = "typebase_premium_unlock"
}
